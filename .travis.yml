language: generic 
sudo: required
services:
    - docker

before_install:
    - docker build -t yungshin/docker-react -f Dockerfile.dev .
    
script:
    - docker run -e CI=true yungshin/docker-react npm run test -- --coverage